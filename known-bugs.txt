And/OR connections don't highlight on first click? dunno why lmao

Deleting filters doesn't correctly update the components

How should default grid state be achieved, probably via building a grid?

Routing is a bit stinky, in that it doesn't work